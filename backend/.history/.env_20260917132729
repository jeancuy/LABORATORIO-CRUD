# Ambiente de desarrollo
NODE_ENV=development
PORT=3000

# MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=laboratorio_crud

# JWT - Cambia este valor en tu entorno real
JWT_SECRET=clave_secreta_de_desarrollo
JWT_EXPIRES_IN=2h

# Admin inicial (opcional): si se define, se crea automáticamente al arrancar
# el servidor por primera vez contra una base de datos nueva. Si ya existe un
# usuario con ese email, no se vuelve a crear.
ADMIN_NAME=Administrador
ADMIN_EMAIL=admin@labcrud.local
ADMIN_PASSWORD=Admin123*